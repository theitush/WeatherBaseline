# Secrets — CDS API credentials. NEVER commit.
.cdsapirc

# Python env / caches
.venv/
__pycache__/
*.pyc

# Large source rasters & docs (re-downloadable from Copernicus GHSL)
*.tif
*.tif.ovr
*.pdf
*.xlsx
*:Zone.Identifier

# Scratch output
.bench_tmp/

# Note: benchmark_zarr_results*.json are committed intentionally as the
# decision record for why zarr was chosen over the CDS API (see STATUS.md).

# Snap-distance audit: cached per-tile land masks + report (regenerable)
snap_audit_masks/
snap_audit.csv

# Run artefacts from top-up / repair passes: tile & cell worklists, resume
# logs, pid files. Regenerated per run, meaningless to anyone else.
gap_cells_*.txt
gap_tiles_*.txt
repull_tiles.txt
orphan_archives.txt
topup_*_remaining_tiles.txt
topup_*.log
topup_*.pid
