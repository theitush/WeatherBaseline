{
  "name": "howhot-weather-app",
  "version": "1.0.0",
  "description": "Weather visualization app with temperature history",
  "main": "backend/server.js",
  "scripts": {
    "start": "bash -c 'source $HOME/.nvm/nvm.sh && nvm use --silent 18 && node backend/server.js'",
    "dev": "bash -c 'source $HOME/.nvm/nvm.sh && nvm use --silent 18 && node backend/server.js'",
    "dev:frontend": "bash -c 'source $HOME/.nvm/nvm.sh && nvm use --silent 22 && cd frontend && npm run dev'",
    "dev:all": "bash -c 'npm run dev & npm run dev:frontend & wait'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0",
    "playwright": "^1.58.2"
  },
  "keywords": [
    "weather",
    "visualization",
    "temperature"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0"
  }
}
